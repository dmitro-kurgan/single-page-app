{
  "name": "single-page-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "node-sass src/scss/style.scss -o dist/css --output-style expanded && babel src/js -d dist/js --presets es2015",
    "watch": "npm run build && node-sass src/scss/style.scss -wo dist/css && babel src/js -d dist/js --presets es2015 -w"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-es2015": "^6.24.1",
    "node-sass": "^4.9.3"
  }
}
